# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CollectionViewInTableViewCell' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CollectionViewInTableViewCell
    pod 'Alamofire', '~> 4.4'
    pod 'Gloss', '~> 2.0'
    pod 'Kingfisher', '~> 5.0'

  target 'CollectionViewInTableViewCellTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CollectionViewInTableViewCellUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
